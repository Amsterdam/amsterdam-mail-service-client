# NOTE: This file is auto generated by OpenAPI Generator.
# URL: https://openapi-generator.tech
#
# ref: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: amsterdam_mail_service_client Python package

on: [pull_request]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4
      - name: Pull services
        run: |
          docker pull amsterdam/amsterdam-mail-service:latest
          docker pull axllent/mailpit:latest
      - name: Start mail services
        run: |
          docker run -itd --network=host -e PORT=8003 -e SMTP_HOST=localhost -e SMTP_PORT=1025 -p 8003:8003 amsterdam/amsterdam-mail-service:latest
          docker run -itd --network=host -p 1025:1025 axllent/mailpit:latest
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r test-requirements.txt
      - name: Test with pytest
        run: |
          pytest --cov=amsterdam_mail_service_client
